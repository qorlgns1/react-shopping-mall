import styled from 'styled-components';

const Wrapper = styled.ul`
  margin: 0 auto;
  padding: 80px 0 180px;
  max-width: 1280px;
  display: flex;
  flex-wrap: wrap;
  gap: 70px;
`;

export { Wrapper };
